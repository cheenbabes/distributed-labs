services:
  # ===========================================
  # Jupyter Lab — ML Workspace
  # ===========================================

  jupyter:
    build:
      context: .
    container_name: ml-lab-02-jupyter
    ports:
      - "8888:8888"
    volumes:
      - ./notebooks:/lab/notebooks
      - ./solutions:/lab/solutions
    networks:
      - ml-lab-02-network
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8888/api/status')"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  ml-lab-02-network:
    driver: bridge
