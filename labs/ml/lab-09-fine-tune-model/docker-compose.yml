services:
  # ===========================================
  # Jupyter Lab — Fine-Tuning Workspace
  # ===========================================

  jupyter:
    build:
      context: .
    container_name: ml-lab-09-jupyter
    ports:
      - "8888:8888"
    volumes:
      - ./notebooks:/lab/notebooks
      - ./solutions:/lab/solutions
    networks:
      - ml-lab-09-network
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8888/api/status')"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  ml-lab-09-network:
    driver: bridge
